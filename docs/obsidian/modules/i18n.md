---
module: I18n
symbols: 3
cohesion: 100%
source: gitnexus_cypher (cluster="I18n")
---

# Module: I18n — homepage layout + localized copy

> Small, high-cohesion cluster bridging homepage rendering and locale-specific copy strings.

## Code location

| File | Purpose |
| --- | --- |
| `frontend/src/components/page-layouts/HomePage.tsx` | Homepage layout component |
| `frontend/src/lib/hooks/useHomeSections.ts` | Hook that resolves which home sections to render per locale |
| `frontend/src/lib/i18n/home.ts` | Locale-specific homepage copy strings (`getHomeStrings`) |

## Members (3)

| Symbol | Kind | File |
| --- | --- | --- |
| `HomePage` | Function | `frontend/src/components/page-layouts/HomePage.tsx` |
| `useHomeSections` | Function | `frontend/src/lib/hooks/useHomeSections.ts` |
| `getHomeStrings` | Function | `frontend/src/lib/i18n/home.ts` |

## Cohesion: 100%

All three members are tightly coupled — `HomePage` consumes `useHomeSections`, which in turn depends on `getHomeStrings` for localized copy. No edges leak outside the cluster.

## Why this is a separate cluster

The community detector isolated these 3 symbols from the broader `Page-layouts` and `Cms` clusters because they form a tight loop around homepage-specific i18n logic that doesn't apply to other page types.

## Related

- [[page-layouts]] — sibling page-shape components (Standard, Contact, Gallery, etc.)
- [[cms]] — gateway layer that feeds data into `HomePage`
- [[00-MOC-Frontend]] — frontend entry points
