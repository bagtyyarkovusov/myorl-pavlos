{
  "name": "myorl-pavlos",
  "private": true,
  "description": "myORL medical website — Strapi CMS + Next.js frontend monorepo",
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up -d",
    "dev:local": "npx concurrently -n strapi,nextjs -c blue,green \"npm run develop --prefix backend\" \"npx wait-on tcp:localhost:1337 && npm run dev --prefix frontend\"",
    "dev:db": "docker compose -f docker-compose.dev.yml up -d postgres",
    "dev:down": "docker compose -f docker-compose.dev.yml down",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "start": "docker compose -f docker-compose.dev.yml up",
    "stop": "docker compose -f docker-compose.dev.yml down",
    "transfer:push": "docker exec myorl-strapi-dev npx strapi transfer --to ${STRAPI_PROD_URL} --to-token ${STRAPI_TRANSFER_TOKEN} --force",
    "transfer:pull": "docker exec myorl-strapi-dev npx strapi transfer --from ${STRAPI_PROD_URL} --from-token ${STRAPI_TRANSFER_TOKEN} --force",
    "test": "npm run test --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "typecheck": "npm run typecheck --prefix frontend",
    "format:check": "npm run format:check --prefix frontend",
    "db:provision": "python3 tools/run_postgres_rehearsal.py",
    "db:pull:prod": "scripts/railway-pull-db-to-docker.sh",
    "gate:readiness": "python3 tools/production_readiness_gate.py",
    "gate:nextjs": "python3 tools/nextjs_readiness_gate.py",
    "audit:content": "python3 tools/audit_nextjs_content_hygiene.py",
    "e2e": "npm run e2e --prefix frontend",
    "e2e:ui": "npm run e2e:ui --prefix frontend",
    "e2e:report": "npm run e2e:report --prefix frontend",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
