---
date: 2026-04-30
indexed-commit: e5da5d6
risk-level: CRITICAL
source: gitnexus_detect_changes + cluster + impact analysis
---

# Codebase audit — 2026-04-30

> Generated from GitNexus graph data, not from a manual code review. Numbers reflect what is currently indexed; see [[../gitnexus-state|GitNexus state]] for caveats.

## Headline

- **Working tree is in CRITICAL state.** 84 changed symbols in 47 files affect 47 indexed processes — including every page-rendering flow and the entire Strapi normalization pipeline. None are committed.
- **`_archived` is the largest cluster** (246 symbols, 79% cohesion) and is being torn out in the same uncommitted change. It still inflates query results.
- **Minified vendor blob is being indexed**: `tailwind-browser.js` contributes 267 symbols and dominates "top callers" rankings. Filter or ignore it.
- **Only 2 API routes** are indexed (`/api/health`, `/api/revalidate`). Both have zero internal consumers — they are externally driven.

## 1. Uncommitted change risk: CRITICAL

`gitnexus_detect_changes(scope: "unstaged")` reports:

| Metric | Value |
| --- | --- |
| Changed symbols | 84 |
| Affected symbols (transitive) | 47 |
| Changed files | 47 |
| Risk level | **critical** |

### Hot files in the diff

- `frontend/src/lib/cms/cms-gateway.ts` — `unwrapStrapiData`, `normalizeEntity`, `deepUnwrapStrapiRelations`, `flattenAttributes` all touched. This is the **core Strapi normalization pipeline** ([[../processes/cms-gateway-pipeline|trace]]).
- `frontend/src/lib/cms/env.ts` — `getCmsConfig`, `parsed` touched. `getCmsConfig` has **CRITICAL** upstream impact (8 direct callers, 8 processes).
- `frontend/src/app/[locale]/page.tsx`, `frontend/src/app/[locale]/[slug]/page.tsx` — `LocaleHomePage`, `CmsPage`, both `generateMetadata` functions touched (page entry points).
- `frontend/src/components/SiteHeaderClient.tsx`, `frontend/src/components/SiteHeader.tsx`, `frontend/src/components/site-header/internal/MegaMenu.tsx` — header rewrite in progress.
- `frontend/src/components/sections/SectionRenderer.tsx` — `SectionRenderer`, `renderSectionBody`, `renderSectionBodyHome`, `DisclosureList`, `ResponsiveImage` all touched.
- `frontend/src/components/page-layouts/*` — every page layout (`HomePage`, `StandardPage`, `AppointmentPage`, `ContactPage`, `FrontendNativePage`, `GalleryPage`, `QuestionListPage`, `_shared`) modified.
- `frontend/src/components/home/*` — every Home section (`HomeHero`, `HomePromoCarousel`, `HomeMedicalLedger`, `HomeContactFooter`, `HomeAdvantagesSection`, `HomeVideoTheater`).
- `tools/nextjs_readiness_gate.py` — `strapi_origin` touched.
- Backend component blocks (10 files under `backend/src/components/blocks/*.json` and `backend/src/components/shared/location.json`) **deleted**, plus three top-level helper Python scripts deleted.

### Affected processes (top breakage paths)

47 indexed flows are touched. The most severe are flows where a changed symbol is **step 1** (entry point):

| Process | Step 1 symbol | Steps |
| --- | --- | --- |
| `LocaleLayout → *` (5 flows) | `LocaleLayout` | 4–5 |
| `LocaleHomePage → *` (4 flows) | `LocaleHomePage` | 3–4 |
| `GenerateMetadata → *` (12 flows) | `generateMetadata` (both `[locale]` and `[locale]/[slug]`) | 4 |
| `CmsPage → *` (3 flows) | `CmsPage` | 4 |
| `Sitemap → *` (5 flows) | `sitemap` | 3 |
| `SectionRenderer → RenderSectionBody` | `SectionRenderer`, `renderSectionBody`, `renderSectionBodyHome` | 3 |
| `SiteHeader → FindInTree`, `SiteHeaderClient → Set`, `MegaMenu → TopicsLabel` | `SiteHeader`, `SiteHeaderClient`, `MegaMenu` | 3 |
| `FetchAllImpl → FlattenAttributes`, `FetchOneImpl → FlattenAttributes`, `CreateCmsGateway → FlattenAttributes` | `unwrapStrapiData`, `normalizeEntity`, `deepUnwrapStrapiRelations`, `flattenAttributes` | 5 |

### Recommended action

1. **Run targeted impact** on `getCmsConfig`, `flattenAttributes`, `LocaleLayout`, `generateMetadata`, `SectionRenderer` — already done below.
2. **Land the deletes first** as a separate commit (component blocks + `_archived` removals) so the diff against the index becomes interpretable.
3. **Re-index after each chunk lands**: `npx gitnexus analyze`.
4. **Do not commit** until `gitnexus_detect_changes` reports a manageable scope — current 47 affected processes is too wide for a single commit.

## 2. High-impact symbols (upstream blast radius)

Pulled via `gitnexus_impact(direction: "upstream", maxDepth: 2)`.

### `getCmsConfig` (frontend/src/lib/cms/env.ts) — risk: **CRITICAL**

- 8 direct callers, 8 processes affected, 3 modules hit (`Cms`, `Navigation`, `Revalidate`).
- Direct callers (d=1): `sitemap`, `robots`, `resolveMediaUrl`, `toPageDTO`, `toPageMetadata`, `GET /api/health`, `POST /api/revalidate`, and `cms-gateway-setup.ts` itself.
- d=2: `createCmsGateway`, both `generateMetadata`, `toMediaDTO`, `cms-api.ts`, `client.ts`.
- This is **the** chokepoint for CMS configuration. Any signature change cascades into every page render, sitemap, robots.txt, and the revalidation webhook.

### `flattenAttributes` (frontend/src/lib/cms/cms-gateway.ts) — risk: **HIGH**

- 2 direct callers (`deepUnwrapStrapiRelations`, `normalizeEntity`), 3 processes (`fetchAllImpl`, `fetchOneImpl`, `createCmsGateway`).
- Currently being modified in the WIP — verify the data shape it returns is unchanged from the consumer perspective, or all CMS reads break.

## 3. Module landscape (151 communities → 12 sized clusters)

Sorted by symbol count. Cohesion is the share of intra-module edges; high = self-contained, low = leaky.

| Module | Symbols | Cohesion | Notes |
| --- | --- | --- | --- |
| `_archived` | 246 | 79% | Old MODX migration scripts under `tools/_archived/`. **Largest cluster — pure dead weight in the graph.** |
| `Claude-design` | 151 | **48%** | Almost entirely `tailwind-browser.js`. Index noise; low cohesion confirms it. |
| `Tools` | 90 | 91% | Active Python migration / readiness tooling under `tools/`. |
| `Cms` | 62 | 76% | Strapi DTO + gateway layer (`frontend/src/lib/cms/`). Contract boundary per ADR-001. |
| `Scripts` | 42 | 86% | `backend/scripts/*.js` one-off cleanup tasks. |
| `Examples` | 22 | 87% | — |
| `Bootstrap` | 13 | 67% | Strapi seeding (`backend/src/bootstrap/`) + a couple of misclassified frontend symbols. |
| `Navigation` | 10 | 81% | `SiteHeader`, route entry points (`generateMetadata`, `LocaleHomePage`, `CmsPage`), `appointment-href`. |
| `Internal` | 10 | 94% | Site-header internals (`MegaMenu`, `useDrawer`, `usePill`, `useNavigationState`). |
| `Components` | 9 | 89% | Design-system + HTML sanitization. |
| `Revalidate` | 9 | 74% | `/api/revalidate` route handler split. |
| `Page-layouts` | 6 | 100% | `_shared`, `GalleryPage`, `QuestionListPage` only. **The other 6 layouts are NOT clustered here** — they are scattered, suggesting weak cohesion in `frontend/src/components/page-layouts/`. |

### Module-level findings

- **Page-layouts cluster only contains 3 files** but the directory has 9. Either the others got assigned to neighbour clusters (Navigation, Components) or they have so few outgoing internal edges that the community detector dropped them. Worth investigating in [[../modules/page-layouts]].
- **Bootstrap cluster includes `tailwind-browser.js` symbols** (`u`, `has`) — that confirms the minified blob bleeds into other clusters via spurious edges.
- **`_archived` cohesion 79%** means even the dead code has 21% leaky edges — it still references active modules. Check before deleting.

## 4. API surface

`gitnexus_route_map` reports only 2 routes:

| Route | Handler | Middleware | Consumers | Flows |
| --- | --- | --- | --- | --- |
| `GET /api/health` | `frontend/src/app/api/health/route.ts` | — | 0 | `GET → NormalizeOrigin` |
| `POST /api/revalidate` | `frontend/src/app/api/revalidate/route.ts` | — | 0 | 6 flows (auth, payload parsing, tag derivation) |

Findings:
- **Zero internal consumers** for both — they are server-only endpoints driven externally (Strapi webhook, monitor probes).
- **No middleware wrappers detected** by the indexer. The revalidate route does its own bearer-secret check inline (`resolveProvidedSecret`, `readBearerSecret` in [[../modules/revalidate]]). If you intend to add `withAuth`-style wrappers, this is where they would land.
- The headless Next.js + Strapi setup means the actual API surface lives in the **Strapi backend**, which contributes 0 routes to this view because GitNexus only crawls Next.js route handlers as `Route` nodes here.

## 5. Process landscape (238 flows total)

Top 5 ranked by step count:

1. `Main → Transliterate` — 7 steps, cross-community (legacy import path).
2. `Apply_dead_html_cleanup_to_resource → _norm_href_key` — 7 steps (legacy).
3. `Main → _collapse_to_uid_segment` — 6 steps (legacy, two variants).
4. `ToPageDTO → NormalizeOrigin` — 6 steps. **Active** — page DTO build path.
5. Multiple 5-step flows: legacy `Main →` flows, plus active `LocaleLayout → *`, `FetchAllImpl → FlattenAttributes`, `FetchOneImpl → FlattenAttributes`.

The longest active execution flow is **`ToPageDTO → NormalizeOrigin`** (6 steps) and the **Strapi normalization pipeline** has multiple 5-step branches all ending at `flattenAttributes`. See [[../processes/cms-gateway-pipeline]].

## 6. Recommendations (prioritised)

1. **Stop indexing minified vendor JS.** Add `artifacts/design-references/**` to the GitNexus ignore config. Removes 267 noise symbols and the bogus `Claude-design` / `Bootstrap` cluster contamination.
2. **Land the WIP in chunks.** Current 47-file diff is too wide to review or impact-check meaningfully. Split: (a) deletes, (b) cms-gateway/env changes, (c) header rewrite, (d) page-layout/section refactor, (e) home sections.
3. **Re-index after every chunk** so impact analysis stays trustworthy.
4. **Promote `getCmsConfig` to first-class.** It is the single most-depended-on symbol in the active code. Document its return shape contract; consider an internal type alias to make signature drift visible.
5. **Confirm Strapi pipeline parity.** With `unwrapStrapiData`, `normalizeEntity`, `deepUnwrapStrapiRelations`, `flattenAttributes` all touched simultaneously, write an integration test (real Strapi response → expected DTO) before the diff merges.
6. **Decide on `_archived/`.** Either delete (and re-index) or move outside the repo. Keeping it inflates impact analysis.

## Related

- [[../gitnexus-state]] — current index state and known gaps
- [[../00-MOC-CodeIntelligence]] — index of graph-derived docs
- [[../modules/cms]], [[../modules/navigation]], [[../modules/revalidate]]
- [[../processes/cms-gateway-pipeline]], [[../processes/page-rendering]]
