# Design References

Archived frontend design references live here so app tooling under `frontend/`
does not lint or format generated standalone prototypes.

The current archived prototype folder is `claude-design/`.
