---
module: I18n
symbols: 2
cohesion: 100%
source: gitnexus_cypher (cluster="I18n")
---

# Module: I18n — homepage layout + localized copy

> Small cluster bridging homepage rendering and locale-specific copy strings. After the section pipeline unification, `useHomeSections` was deleted and home section dispatch moved into `SectionRenderer.tsx`.

## Code location

| File | Purpose |
| --- | --- |
| `frontend/src/components/page-layouts/HomePage.tsx` | Homepage layout component |
| `frontend/src/lib/i18n/home.ts` | Locale-specific homepage copy strings (`getHomeStrings`) |

## Members (2)

| Symbol | Kind | File |
| --- | --- | --- |
| `HomePage` | Function | `frontend/src/components/page-layouts/HomePage.tsx` |
| `getHomeStrings` | Function | `frontend/src/lib/i18n/home.ts` |

`getHomeStrings` is also consumed by `renderSectionBodyHome` in `SectionRenderer.tsx` to provide i18n strings for home-specific section variants.

## Cohesion: 100%

`HomePage` consumes `getHomeStrings` for hero, footer, and section fallback copy. No edges leak outside the cluster.

## Related

- [[page-layouts]] — sibling page-shape components (Standard, Contact, Gallery, etc.)
- [[cms]] — gateway layer that feeds data into `HomePage`
- [[sections]] — SectionRenderer dispatches home sections and uses `getHomeStrings`
- [[00-MOC-Frontend]] — frontend entry points
