services:
  postgres:
    image: postgres:18
    container_name: myorl-pg-prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-strapi}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-strapi}
    volumes:
      - pgdata-prod:/var/lib/postgresql
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-strapi} -d ${POSTGRES_DB:-strapi}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - internal

  strapi:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: myorl-strapi-prod
    environment:
      HOST: 0.0.0.0
      PORT: 1337
      DATABASE_CLIENT: postgres
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_NAME: ${POSTGRES_DB:-strapi}
      DATABASE_USERNAME: ${POSTGRES_USER:-strapi}
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_SSL: "false"
      STRAPI_CORS_ORIGINS: ${STRAPI_CORS_ORIGINS}
      APP_KEYS: ${APP_KEYS}
      API_TOKEN_SALT: ${API_TOKEN_SALT}
      ADMIN_JWT_SECRET: ${ADMIN_JWT_SECRET}
      TRANSFER_TOKEN_SALT: ${TRANSFER_TOKEN_SALT}
      JWT_SECRET: ${JWT_SECRET}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      STRAPI_REVALIDATE_SECRET: ${STRAPI_REVALIDATE_SECRET}
      NEXT_REVALIDATE_URL: ${NEXT_REVALIDATE_URL:-http://nextjs:3000/api/revalidate}
      STRAPI_PUBLIC_URL: ${STRAPI_PUBLIC_URL:-}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - uploads:/app/public/uploads
    restart: unless-stopped
    networks:
      - internal

  meilisearch:
    image: getmeili/meilisearch:v1.11
    container_name: meilisearch
    environment:
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY}
    volumes:
      - meilidata_prod:/meili_data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:7700/health || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - internal

  nextjs:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    container_name: myorl-nextjs-prod
    environment:
      STRAPI_URL: ${STRAPI_URL:-http://strapi:1337}
      STRAPI_TOKEN: ${STRAPI_TOKEN}
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
      STRAPI_REVALIDATE_SECRET: ${STRAPI_REVALIDATE_SECRET}
      SEARCH_ENABLED: ${SEARCH_ENABLED:-false}
      NEXT_PUBLIC_SEARCH_ENABLED: ${SEARCH_ENABLED:-false}
      MEILI_HOST: ${MEILI_HOST:-http://meilisearch:7700}
      NEXT_PUBLIC_MEILI_HOST: ${NEXT_PUBLIC_MEILI_HOST}
      MEILI_MASTER_KEY: ${MEILI_MASTER_KEY}
      NEXT_PUBLIC_MEILI_SEARCH_KEY: ${NEXT_PUBLIC_MEILI_SEARCH_KEY}
      STRAPI_WEBHOOK_SECRET: ${STRAPI_WEBHOOK_SECRET}
    depends_on:
      - strapi
      - meilisearch
    restart: unless-stopped
    networks:
      - internal

  caddy:
    image: caddy:2
    container_name: myorl-caddy-prod
    environment:
      CADDY_DOMAIN: ${CADDY_DOMAIN}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - nextjs
      - strapi
    restart: unless-stopped
    networks:
      - internal

volumes:
  pgdata-prod:
    name: myorl-pavlos_pgdata-prod
  uploads:
    name: myorl-pavlos_uploads
  meilidata_prod:
    name: myorl-pavlos_meilidata_prod
  caddy-data:
    name: myorl-pavlos_caddy-data
  caddy-config:
    name: myorl-pavlos_caddy-config

networks:
  internal:
    driver: bridge
