# Source Data

Stable MODX and Strapi migration inputs belong here.

## Layout

- `modx/` contains stable MODX source snapshots and transformed migration input.
- `checkpoints/` contains importer checkpoint/state maps used for idempotent reruns.
