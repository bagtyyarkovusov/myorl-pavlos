#!/usr/bin/env sh
# Run lint-staged for files inside frontend/. Skips silently if the staged set
# does not touch the frontend package.
cd "$(dirname "$0")/.." || exit 0
if git diff --cached --name-only | grep -q '^frontend/'; then
  npx lint-staged
fi
